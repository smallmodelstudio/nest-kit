{
  "name": "@smallmodelstudio/nest-bootstrap",
  "version": "0.0.0",
  "description": "createApp(AppModule, { plugins }), shared by main.ts and createTestApp so they can't drift apart.",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=24"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@nestjs/common": "^12",
    "@nestjs/core": "^12",
    "@nestjs/platform-fastify": "^12",
    "fastify": "^5"
  },
  "devDependencies": {
    "@nestjs/common": "^12.0.3",
    "@nestjs/core": "^12.0.3",
    "@nestjs/platform-fastify": "^12.0.3",
    "@types/node": "24.13.4",
    "fastify": "^5.12.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  }
}
