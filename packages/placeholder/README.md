# @smallmodelstudio/placeholder

Placeholder package used to prove the nest-kit workspace pipeline (lint,
typecheck, test, build, export checks, layer check, standalone install and
boot) end to end. It has no runtime purpose and is removed once real packages
exist.
