{
  "name": "playground",
  "private": true,
  "version": "0.0.0",
  "description": "Nest app proving the contract model end to end (docs/README-roadmap.md#1-core-spike).",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "pnpm run build && node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/static": "^10.1.3",
    "@nestjs/common": "^12.0.3",
    "@nestjs/core": "^12.0.3",
    "@nestjs/platform-fastify": "^12.0.3",
    "@nestjs/swagger": "^12.0.1",
    "@smallmodelstudio/contract": "workspace:*",
    "@smallmodelstudio/nest-envelope": "workspace:*",
    "@smallmodelstudio/nest-errors": "workspace:*",
    "@smallmodelstudio/nest-zod": "workspace:*",
    "fastify": "^5.12.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@nestjs/testing": "^12.0.3"
  }
}
